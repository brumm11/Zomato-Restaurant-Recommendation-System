# Phase 1: Foundation and Environment Setup

Contains:
- API bootstrap (`api/app.py`)
- core settings, logging, and error models (`core/`)

Purpose:
- establish baseline backend structure
- expose `GET /health`
- provide reusable core primitives for later phases
