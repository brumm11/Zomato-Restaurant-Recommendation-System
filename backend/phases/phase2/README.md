# Phase 2: Data Ingestion, Cleaning, and Canonical Modeling

Contains:
- normalized data models (`data/models.py`)
- ingestion and normalization pipeline (`data/pipeline.py`)
- executable ingestion job (`data/run_ingestion.py`)

Outputs:
- `artifacts/data/restaurants_normalized.jsonl`
- `artifacts/data/data_quality_report.json`
