# Copy to `.env` and set real values. Do not commit secrets.

APP_NAME=restaurant-recommendation-service
APP_ENV=development

# Groq LLM (Phase 3+). Get a key at https://console.groq.com/
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# Legacy alias if code still reads these names:
LLM_API_KEY=
LLM_MODEL=

DATA_SOURCE=huggingface:ManikaSaini/zomato-restaurant-recommendation
NORMALIZED_DATA_PATH=artifacts/data/restaurants_normalized.jsonl
